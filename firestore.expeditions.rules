rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Allow full access to expeditions collection
    match /expeditions/{expeditionId} {
      allow read, write: if true;
    }
  }
}
